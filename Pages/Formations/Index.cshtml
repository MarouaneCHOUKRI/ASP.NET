@page
@model Universite.Pages.Formations.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Formation[0].Intitule)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Formation[0].AnneeDiplome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Formation[0].NbInscrits)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Formation[0].NbUes)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Formation) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Intitule)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AnneeDiplome)
            </td>
            <td>
                  @Html.DisplayFor(modelItem => item.NbInscrits)
            </td>
            <td>
                  @Html.DisplayFor(modelItem => item.NbUes)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
